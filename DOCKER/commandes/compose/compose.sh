# Pour lancer un Docker Compose (dans le répertoire où se trouve le fichier docker-compose.yml)
docker compose up

# Même chose en tâche de fond
docker compose up -d

# Pour arrêter un Docker Compose (dans le répertoire où se trouve le fichier docker-compose.yml)
docker compose down
